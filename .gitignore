node_modules/
dist/
.env
.env.local
# Backups written by set-turn.ps1 and by hand. These hold the same secrets
# as .env, so they must never be committed either.
.env.bak
.env.*.bak
.env.bak.*
*.log
.vercel/
.DS_Store

# Gender-inference weights: fetched by `npm run models:fetch`, not committed.
server/models/*.onnx
server/models/fixtures/
server/prisma/*.db
